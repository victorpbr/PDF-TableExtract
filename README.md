Implementation of the tabula-py package allowing user to select visually the area of interest and the columns positions.

poppler is required to run. It's location needs to be adjusted on the class file.

package versions used:
  tabula-py 2.6.0
  poppler 22.11.0

